# 本机验证记录

已完成以下检查：

1. 四项物理/输入单元测试通过：绝热均匀加热解析解（含非整除时间间隔及启停）、初始温度等于环境时保持恒温、线性温度场剖面精确插值、非法数值与超限时间设置被拒绝。
2. 新导入 20 mm 立方体 STL 与 STEP，实际完成两次铜铝双材料瞬态求解。STEP 两个材料区域体积分数各为 0.5；STL 基于单元归属约为 0.502 / 0.498。热源 2 W、2–18 s 开启，输入热量均为 32 J，能量平衡通过。
3. 实际完成悟空模型的铜/铝/铁三区 STEP 分割和求解：23,802 节点、96,165 四面体，100 W 加热 120 s，输入 12,000 J。该算例早期存在约 0.0021°C 的小幅离散温度下冲，保留原值并在结果说明中提示复核。
4. 本地 API 验证模型单位、保存与重新加载算例、无效面编号拒绝、跨站写入拒绝、结果 ZIP 与真实体积截面。
5. 在本地浏览器实际操作热源刷选，新增选区得到 23 个三角面；验证材料区域查看、温度时间轴与内部剖切，并通过“运行仿真”按钮重新计算 STEP 双材料算例，页面显示新结果。
6. 页面没有浏览器错误日志。Three.js 和 OrbitControls 已保存在本地，运行界面无需访问 CDN。
7. 新增稳态求解、辐射线性化边界、功率曲线、温控启停、相变等效热容和等效接触热阻验证；带扩展物理设置的瞬态算例完成，能量残差通过。
8. 新增换热系数反推、安全功率优化和制冷循环 COP API 验证；本地主页返回 200，健康检查和分析接口可用。
9. 复杂多组件 STL `flexible_jumpsuit.stl` 改用离散拓扑体网格路径后完成实际求解：21,191 节点、70,257 四面体、61 帧；仅保留网格质量提示。

这些是实现与数值一致性验证，不替代具体工程工况的实验校准及网格/时间收敛分析。制冷循环页面采用理想温差模型，工程设计仍需工质物性和设备性能曲线。精确参数及计算记录见 validation-api.json、validation-wukong.json 和 data/jobs。
